#include <stdio.h>
int main (){
	int abcd;
	printf("Nhap chu so: ");
	scanf("%d",abcd);
	int total = a + b + c + d ;
	int inverse = d*1000 + c*100 + b*10 + a;

	printf("Tong chu so la :%d va nghich dao la:%d",total, inverse );
}
